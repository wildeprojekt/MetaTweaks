/**
 * Mixins applied by MetaTweaks to modify vanilla and mod behavior in a server-friendly way.
 * <p>
 * Each mixin targets a specific system (e.g., concrete powder hardening, water freezing, farm/grass spread,
 * projectile behavior, etc.) to enforce server build rules and prevent griefing.
 */
package com.wildeprojekt.metatweaks.mixin;
